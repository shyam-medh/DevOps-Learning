apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  # Replace this base64 placeholder with your own value before deploying.
  MYSQL_ROOT_PASSWORD: REPLACE_WITH_BASE64_PASSWORD
